"""FastAPI Service Layer for the NCERT Educational Tutor (Phase 8.0).

Exposes the frozen ``EducationalTutorEngine`` and ``LearningSessionEngine`` behind a
clean REST API. No educational logic lives here — only configuration, dependency
injection, request/response mapping, and exception handling.
"""
